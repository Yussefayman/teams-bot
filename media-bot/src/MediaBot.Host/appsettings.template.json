{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "Real values come from environment variables (plan §13). Copy to appsettings.json only for non-secret local defaults; never commit secrets.",
  "Bot": {
    "MediaPort": 8445,
    "DumpDir": "data/dumps",
    "ReconnectBufferSeconds": 60
  }
}
