# COMPONENTS C + D — orchestrator runtime deps (plan §6). Milestone M3+.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
httpx>=0.27
jsonschema>=4.20         # validate MoM + call_event against shared/schemas
msal>=1.31               # Graph app-only auth (client credentials)
openai>=1.50             # Azure OpenAI client
tzdata>=2024.1           # zoneinfo for Asia/Riyadh date resolution
