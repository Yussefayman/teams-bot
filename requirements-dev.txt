# Repo-wide dev/test tooling (lightweight, cross-platform). Installed into .venv.
# Heavy service runtime deps live in each service's own requirements.txt.
jsonschema>=4.20
pytest>=8.0
websockets>=12.0
httpx>=0.27
