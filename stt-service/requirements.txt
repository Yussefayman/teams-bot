# COMPONENT B — STT service runtime deps (plan §5).
# These are heavy (GPU/ML) and belong to milestone M2. Do NOT install on a dev Mac
# just for M0/M1 — install on the GPU host when STT work starts.
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
pydantic-settings>=2.4
faster-whisper>=1.0      # Whisper large-v3 inference
# silero-vad is fetched at runtime via torch.hub, or pin a packaged build:
# silero-vad>=5.1
numpy>=1.26
